# vim: set ft=bash

export HOME="/lfs/local/0/ranjanr"
if [[ -d $HOME ]]
then
	cd
else
	unset HOME
fi

# if not interactive, exit
[[ $- != *i* ]] && return

export SHELL="$HOME/.pixi/bin/fish"
if [[ -x $SHELL ]]
then
	exec $SHELL
else
	unset SHELL
fi
